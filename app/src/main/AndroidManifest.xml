<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.recipe">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-sdk android:minSdkVersion="21" android:targetSdkVersion="33" />

    <!-- Application settings -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Recipe"> <!-- Default theme -->

        <activity android:name=".Login" android:theme="@style/Theme.Recipe.NoActionBar" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" android:theme="@style/Theme.Recipe.NoActionBar" android:exported="true"/>
        <activity android:name=".Profile" android:exported="true" />
        <activity
            android:name=".Signup"
            android:exported="true"
            android:theme="@style/AppTheme"
            />
        <activity android:name=".RecipeDetails" android:exported="true"/>
        <activity android:name=".SubmitRecipe" android:exported="true"/>
        <activity android:name=".ShoppingList" />
        <activity
            android:name=".EditRecipeList"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".EditRecipe"
            android:theme="@style/AppTheme" />
    </application>

</manifest>
